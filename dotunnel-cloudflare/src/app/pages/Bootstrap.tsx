import type * as React from "react";

export function BootstrapPage(): React.ReactElement {
  // 1. Setup auth provider
  // 2. Sign-up initial admin user
  // 3. Setup tunnel routes
  // 4. Setup security policies
  // 5. Finish guide
}
